{% extends 'base.html.twig' %}

{% block title %}Welcome{% endblock %}

{% block body %}

    

<div class="container mx-auto mt-4">
    <div class="row">
        
        <center><h1>{{ categorie.nom }}</h1></center>
        <hr>
           
        {% for sc in sousCategories %}
        <div class="d-flex justify-content-center flex-wrap">
            <div class="card d-flex col-6">
            <a href="{{ path("app_produit", {id: sc.id}) }}">
                <div class="card text-white bg-primary mb-4 ">
                    <div class="card-header d-flex justify-content-center">{{ sc.nom }}</div>
                        <div class="card-body d-flex flex-column align-items-center justify-content-center">
                    <img src="/images/sous_categorie{{ sc.imagesrc }}" alt="souscategorie" style="max-height: 200px; width: 100%;">
                        <p class="card-text text-center">secteur</p>
                        <a href="{{ path("app_produit", {id: sc.id}) }}" class="btn btn-primary">Voir les produits</a>
                        </div>
                    </div>

                </div>
            </a>
            </div>
        </div>
        {% endfor %} 
 
        
    </div>
</div>
  
   
{% endblock %}