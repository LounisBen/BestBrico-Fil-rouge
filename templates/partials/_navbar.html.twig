<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">BestBrico</font></font></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Basculer la navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <a class="nav-link active" href="/">Accueil<span class="visually-hidden"></span></a>
        </li>
        {% for categorie in categories %}
          <li class="nav-item ">
          <a class="nav-link active" href="/categorie/{{categorie.id}}">
          {{ categorie.nom }}
          </a>
          </li>
          {% endfor %}
      </ul>  
      
      <ul class="navbar-nav me-auto">
      {% if app.user %}
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false"><font style="vertical-align: inherit;"><font style="vertical-align: inherit;">{{ app.user.prenom}}</font></font></a>
          <div class="dropdown-menu">
            <a class="dropdown-item" href="{{ path("logout") }}"><font style="vertical-align: inherit;"><font style="vertical-align: inherit;">Me déconnecter</font></font></a>
            <a class="dropdown-item" href="{{ path("user.edit", {id : app.user.id}) }}"><font style="vertical-align: inherit;"><font style="vertical-align: inherit;">Modifier mes informations</font></font></a>
            <a class="dropdown-item" href="{{ path("editPassword", {id : app.user.id}) }}"><font style="vertical-align: inherit;"><font style="vertical-align: inherit;">Changer mon mot de passe</font></font></a>
            <div class="dropdown-divider"></div>
            <a class="dropdown-item" href="#"><font style="vertical-align: inherit;"><font style="vertical-align: inherit;">Lien séparé</font></font></a>
          </div>
        </li>  
      {% else %}
      <li class="navbar-nav me-auto">
          <a class="nav-link active" href="{{ path('login')}}">Connexion</a>
          {# <a class="nav-link active" href="{{ path('registration')}}">Inscription</a> #}
      </li>
      {% endif %}
      </ul>
      <div class="d-flex me-4">
                <a href="{{ path('app_panier')}}" class="text-light fw-bold">
                    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-cart-fill" viewBox="0 0 16 16">
                      <path d="M0 1.5A.5.5 0 0 1 .5 1H2a.5.5 0 0 1 .485.379L2.89 3H14.5a.5.5 0 0 1 .491.592l-1.5 8A.5.5 0 0 1 13 12H4a.5.5 0 0 1-.491-.408L2.01 3.607 1.61 2H.5a.5.5 0 0 1-.5-.5zM5 12a2 2 0 1 0 0 4 2 2 0 0 0 0-4zm7 0a2 2 0 1 0 0 4 2 2 0 0 0 0-4zm-7 1a1 1 0 1 1 0 2 1 1 0 0 1 0-2zm7 0a1 1 0 1 1 0 2 1 1 0 0 1 0-2z"/>
                    </svg>
                </a>
            </div>
      <form class="d-flex">
        
        <input class="form-control me-sm-2" type="search" placeholder="Que recherchez-vous?">
        <button class="btn btn-secondary my-2 my-sm-0" type="submit"><font style="vertical-align: inherit;"><font style="vertical-align: inherit;">Recherche</font></font></button>
      </form>
    </div>
  </div>
</nav>